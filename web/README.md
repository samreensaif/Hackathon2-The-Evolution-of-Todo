# Todo App — Frontend (Next.js)

This folder will contain the Next.js (TypeScript) frontend for Phase II of the Todo app.

Getting started (developer):

1. Install dependencies: `npm install`
2. Run development server: `npm run dev`

Notes:
- The real implementation will include React components, pages, and tests.
- This README is a scaffold placeholder created by T001.
